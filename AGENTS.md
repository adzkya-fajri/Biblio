# About Biblio
An application about online book, it uses Firebase (auth) and Laravel (auth through firebase, databases, cdns).

## Notes
- Keep this rule (AGENTS.md) as high-signal, and READ ONLY
- All files/packages @com.example.biblio.data.remote are generated by OpenApi.
  - Don't change the spec, because it's absolute.
    - Suggestion about the spec is welcome
  - Just follow the spec

## Do's
- Be idealistic (very important)
  - Write code in a semantic, "proper", "systematic", "functional", and "efficient", way
  - If possible use the latest, stable, and "proper" dependencies
  - Documentation
- Be more thoughtful
  - Prepared of what's about to happen (either expected or unexpected) recursively
  - Give reasons of the actions
  - Give suggestions

## Don'ts
- Don't change/delete the documentation of the code
  - Don't delete developer notes
  - Don't delete generated documents, such as OpenAPI spec
- Don't change/delete files that are considered as secrets
  - `google-services.json`
  - `local.properties`
  - etc...
- Don't hardcode things (be idealistic)
- Don't make new file, packages, and dependencies without asking and giving reason
- Don't change/delete/add @com.example.biblio.ui, unless if I asked it to.
- Don't go out of prompt scope. 
  - Unless if the scope is heavily, dependently related, and I asked it.
- DRY (Don't Repeat Yourself)
